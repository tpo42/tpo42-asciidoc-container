.*.sw?
*~
*.bak
*.orig
.DS_Store
# host-local lefthook overrides — where the tools come from is not a repository decision
lefthook-local.yml
# host-local compose values — your git identity, for tools running in a container
.env
build/
# asciidoctor-diagram's image cache, written next to the document it renders
.asciidoctor/
